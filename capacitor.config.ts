import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nailgluecabinet.app',
  appName: 'nailglue-cabinet',
  webDir: 'dist/public'
};

export default config;
